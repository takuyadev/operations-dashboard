# Host (pnpm) dev: the Postgres container publishes 5433 to localhost (see docker-compose.yaml).
# 5433, not 5432, to avoid clashing with a native Postgres install.
DATABASE_URL="postgresql://user:password@localhost:5433/operations-dashboard"

# HTTP + WebSocket port for this server.
PORT=4000
